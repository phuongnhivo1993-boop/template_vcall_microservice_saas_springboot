apiVersion: v1
kind: Secret
metadata:
  name: vcall-db-secret
  namespace: vcall
type: Opaque
stringData:
  username: vcall
  # TODO: Use external secrets manager (e.g. SealedSecrets, Vault, AWS Secrets Manager) in production
  password: vcall@123
